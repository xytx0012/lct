<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xytx.mapper.UFinanceAccountMapper">
  <resultMap id="BaseResultMap" type="com.xytx.moder.UFinanceAccount">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="uid" jdbcType="INTEGER" property="uid" />
    <result column="available_money" jdbcType="DECIMAL" property="availableMoney" />
  </resultMap>
  <sql id="Base_Column_List">
    id, uid, available_money
  </sql>
  <select id="selectMoneyByUid" resultType="java.math.BigDecimal">
    select available_money from u_finance_account where uid=#{uid}
  </select>
  <insert id="insertFinanceAccount" parameterType="com.xytx.moder.UFinanceAccount">
    insert into u_finance_account
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="id != null">
        id,
      </if>
      <if test="uid != null">
        uid,
      </if>
      <if test="availableMoney != null">
        available_money,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="id != null">
        #{id,jdbcType=INTEGER},
      </if>
      <if test="uid != null">
        #{uid,jdbcType=INTEGER},
      </if>
      <if test="availableMoney != null">
        #{availableMoney,jdbcType=DECIMAL},
      </if>
    </trim>
  </insert>
  <update id="updateMoney">
    update u_finance_account set available_money = available_money-#{money} where available_money>=#{money} and uid=#{userId};
  </update>
  <!--收益返还，更新资金-->
  <update id="updateAvailableMoneyByIncomeBack">
    update  u_finance_account set  available_money = available_money + #{bidMoney} + #{incomeMoney}
    where uid = #{uid}
  </update>
  <!--充值更新金额-->
  <update id="updateAvailableMoneyByRecharge">
    update  u_finance_account set  available_money = available_money + #{rechargeMoney}
    where uid = #{uid}
  </update>
</mapper>